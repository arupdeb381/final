# Service routes to BOTH versions
apiVersion: v1
kind: Service
metadata:
  name: app-service
spec:
  selector:
    app: myapp  # Routes to BOTH v1 and v2 pods
  ports:
  - port: 80
    targetPort: 80
  type: ClusterIP
  
