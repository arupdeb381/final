apiVersion: v1
kind: Service
metadata:
  name: user-inventory-web
spec:
  selector:
    app: user-inventory-web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30080
  type: NodePort
